## IsleDB

> The embedded database that scales like object storage.

IsleDB is an embedded storage engine built on WiscKey-style key-value separation. 
Data lives on object storage (S3, GCS, Azure Blob, MinIO). 
Bottomless capacity. High durability. Readers scale horizontally—no replicas, no connection limits.
